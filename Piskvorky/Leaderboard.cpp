#include"Lederboard.h"

#include <iostream>




/*int compare;
	struct leader* novyZaznam;
	struct leader* aktZaznam;

	novyZaznam = (struct leader*)malloc(sizeof(struct leader));

	strcpy_s(novyZaznam->meno, NAME_SIZE, meno);
	novyZaznam->pocetVyhier = pocetVyhier + pocetVyhier;
	novyZaznam->dalsi = NULL;

	if (*uk_prvy == NULL)
	{
		*uk_prvy = novyZaznam;
		return;
	}
	else
		if (novyZaznam->pocetVyhier < (*uk_prvy)->pocetVyhier)
		{
			novyZaznam->dalsi = *uk_prvy;
			*uk_prvy = novyZaznam;
			return;
		}

	aktZaznam = *uk_prvy;
	while (aktZaznam)
	{
		if (aktZaznam->dalsi == NULL)
		{
			aktZaznam->dalsi = novyZaznam;
			return;
		}
		else if (novyZaznam->pocetVyhier >= aktZaznam->pocetVyhier);
		{
			novyZaznam->dalsi = aktZaznam->dalsi;
			aktZaznam->dalsi = novyZaznam;
			return;
		}
		aktZaznam = aktZaznam->dalsi;
	}*/